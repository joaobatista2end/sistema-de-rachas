import { DayOfWeek } from '../../object-values/day';
import { Time } from '../../object-values/time';

export class RegisterCalendarSoccerFieldUseCase {
  private execute(
    daysOfWeek: Array<DayOfWeek>,
    openTime: Time,
    closeTime: Time
  ) {
    //TODO: Register calendar inside database
  }
}
